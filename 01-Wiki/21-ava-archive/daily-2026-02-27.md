# Nightly Mission — Fri 2026-02-27 — Website Progress (ecellglobal.com)

## What I shipped (code)
Goal: a concrete, shippable improvement to ecellglobal.com.

**Change:** SEO + social sharing hardening for the Next.js site:
- Added richer `metadata` in `app/layout.tsx`:
  - `metadataBase` set to `https://ecellglobal.com`
  - canonical URL (`alternates.canonical`)
  - robots directives (index/follow + GoogleBot preview/snippet settings)
  - OpenGraph + Twitter card including a large preview image from `/public/images/...`
- Added **`app/sitemap.ts`** → generates `/sitemap.xml`
- Added **`app/robots.ts`** → generates `/robots.txt` referencing sitemap

**Build verification:** `npm run build` succeeded; Next output shows `/robots.txt` and `/sitemap.xml` generated.

**Commit:** `993a899` — `SEO: add sitemap/robots and richer social metadata`

## Deployment status
Attempted `npx vercel --prod --yes`.

**Blocked:** Vercel CLI returned:
> "The specified token is not valid. Use `vercel login` to generate a new token."

So the code is committed locally, but I could not complete the Vercel production deploy from this machine without refreshing auth.

### Fastest next step (Cem)
From `projects/ecell-website`:
1. `npx vercel login` (or set `VERCEL_TOKEN`)
2. `npx vercel --prod --yes`

(If you want, we can store a long-lived token in 1Password and inject via env for fully autonomous deploys.)

## Files changed
- `app/layout.tsx`
- `app/sitemap.ts` (new)
- `app/robots.ts` (new)
