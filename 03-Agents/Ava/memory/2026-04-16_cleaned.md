# Daily Log — April 16, 2026

## Decisions
- **NFL License:** Renewed as of Apr 15 (Confirmed by Cem).
- **Warehouse Normalization:** Consolidate `Florida` → `FL` across BigQuery and Supabase (Owner: Harry; Confirmed by Cem).
- **Advisor Swap:** Using Codex GPT-5.4 (ChatGPT OAuth) instead of Anthropic Advisor API for PRD reviews to optimize costs.
- **Development Strategy:** Utilizing parallel builds (Codex for Listings Pipeline and Athena for Procurement) to accelerate delivery.

## Deliverables
- **Listings Pipeline:** Full Python pipeline in `~/.openclaw/workspace/projects/listings-pipeline/`. Includes SQLite isolation (US, UK, DE), DuckDB TSV parsing, 8 validation gates, and CSV exports (delta, shipping, compliance, etc.).
- **Procurement Priority Overrides:** Live on GitHub. Includes Supabase migration, TypeScript reorder logic (`resolveMultiplier`), API endpoints, and Cron integration.
- **NFL Contract Master Data:** Extracted to `outputs/nfl_mg_schedule_data.json` with schema in `sql/finance_middleware_schema.sql`. Total contract value: $50K ($25K for FY26, $25K for FY27).
- **Amazon Report Middleware:** Cloud Run endpoint `amazon-report-middleware-175143437106.europe-west1.run.app` validated. Supports OIDC + API key (`sk_live_ecell_2026`).

## Blockers
- **Listings Pipeline Validation:** Real Amazon files (e.g., `US Amazon Active+Listings+Report_04-14-2026.txt`) fail the current filename regex pattern.
- **SSH Access:** Ava cannot SSH to Harry's iMac (`100.91.149.92`). **Resolution needed:** Cem to add public key `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPKp4zqrAjixDeJ1nTMeGg3RmvAJnZ3PIaaVGTOVS5lf openclaw@Cems-Mac-Studio.local` to `~/.ssh/authorized_keys`.
- **SP-API Automation:** Security controls questionnaire is pending Amazon approval (Owner: Patrick).
- **DE Catalog:** Manual download of DE Active Listings report is still required.
- **Warehouse Data:** Harry needs to execute BigQuery and Supabase `UPDATE` queries for `Florida` → `FL` normalization.

## Knowledge
- **`blank_inventory` Schema & Logic:**
    - **Columns:** `item_code`, `warehouse`, `free_stocks`, `sales_last_7_days`, `sales_last_30_days`, `reorder_level`, `product_group`, `on_order`.
    - **Critical Logic:** All queries must filter out `DISC` (discontinued) items (78.2% of inventory). `reorder_level` must be calculated dynamically as only 1.8% are pre-set.
    - **Data Sourcing:** Use BigQuery `blank_inventory` for velocity/sales data; the Supabase `orders` table is incomplete (~30% missing).
- **Procurement Technicals:** Supabase project ID `auzjmawughepxbtpwuhe` (credentials in `TOOLS.md`). Must run migration `00004_priority_overrides.sql` before go-live.

## Carry-forwards
- **Decision Required:** Determine whether to rename Amazon files or update `validation.py` to support the new filename format.
- **Database Tasks:** Run Supabase migration to create `license_mg_schedule` and insert NFL contract data.
- **Automation:** Wire Amazon middleware cron for weekly Saturday 6 AM EST reports.
- **Target Go-Live:** April 28, 2026.