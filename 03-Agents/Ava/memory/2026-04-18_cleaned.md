# April 18, 2026

**Decisions**
- **Weekly Listings Delta Reporting Architecture**: Use BigQuery as the primary layer for raw data, history, and diffing. Supabase will be used only as an optional downstream layer for serving tables if required.
- **Reporting Logic**: Implement weekly listing snapshots using a "first-seen" fallback mechanism to mitigate unreliable marketplace created dates. Data must be reconciled against Slack and email EOD claims.

**Deliverables**
- Enabled Gemini TTS in OpenClaw by setting Google as the active TTS provider.

**Knowledge**
- **System Validation**: Use the command `openclaw doctor --non-interactive` to validate TTS provider changes.

**Carry-forwards**
- Run system validation on the Gemini TTS change.
- Transition the listings delta reporting concept from the "shape" phase to the "plan" phase.