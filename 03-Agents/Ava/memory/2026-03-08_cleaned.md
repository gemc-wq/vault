# 2026-03-08 (Sunday)

**Decisions**
- **Veeqo Implementation:** Cem decided to replace Patrick's team with Veeqo + AI. Target staffing: 3 people (UK: 1, FL: 1, PH: 1) replacing 10+ staff, resulting in ~$43K/yr savings.
- **IT Restructuring:** Chad to take over image tools, hardware, and infrastructure, replacing Dickel's 5-person IT team.
- **Wiki Expansion:** Cem requested extraction of all remaining MD files from Clawdbot Shared Folder and Obsidian vault into the central wiki.

**Deliverables**
- **Wiki Expansion:** Completed extraction of 261 total wiki files across 22 sections (added sections 17-22, including `harry-workspace`, `competitor-research`, and `ava-archive`).
- **Gmail OAuth (gog) Setup:** Successfully configured OAuth Desktop client for Gmail (read-only).
    - **Client ID:** `175143437106-funrdi7s4kh1n1sgefiln1v58fq6mkkg.apps.googleusercontent.com`
    - **JSON Path:** `/Users/openclaw/Downloads/client_secret_175143437106-funrdi7s4kh1n1sgefiln1v58fq6mkkg.apps.googleusercontent.com.json`
    - **Auth Command:** `gog auth add gemc@ecellglobal.com --services gmail --readonly`
- **EOD Automation MVP:** Built `projects/eod-automation/src/eod-parser.js` (Node.js) to parse Slack EOD messages for attendance, brands, and SKU counts.
- **Staff Mapping:** Created `org/slack-user-html-map.md` mapping 17 EOD-relevant staff across Creative, Listings, and Production.
- **IT Handover Archive:** Created `wiki/23-drew-handover/` containing 70+ files regarding database queries, process guides, and cron schedules.

**Blockers**
- **Shopify Routing:** Connecting Shopify to both Veeqo US and UK instances causes duplicate orders; a solution is required before GoHeadCase goes international.
- **Walmart Audit (P0):** Blocked due to incomplete data (only 201/95K rows loaded in `walmart_listings`).
- **BigCommerce API:** Blocked; no API credentials available in `TOOLS.md`.
- **EOD Parser Bugs:** Current version (v1) fails to split multi-person messages and has a heuristic error where PO counts are incorrectly identified as SKU counts.
- **Currency Normalization:** `net_sale_usd` remains NULL; requires execution of normalization SQL in Supabase.

**Knowledge**
- **Veeqo Architecture & Pipeline:** Veeqo acts as the intake point (connects to channels, not an API endpoint). 
    - **Flow:** Orders $\rightarrow$ Veeqo $\rightarrow$ Operator sorts by SKU $\rightarrow$ Bulk print labels $\rightarrow$ Veeqo updates marketplaces/notifies customers $\rightarrow$ CSV export drives PO creation for print files.
    - **Smart Order Routing:** Built-in; allocates to the nearest warehouse with stock without needing external APIs.
    - **Reconciliation:** 1:1 ratio between Veeqo shipped items CSV and print file PO creation.
- **IT Infrastructure (Handover):** System includes 2 EC2 instances, RDS database, and BigQuery (`zero_dataset.orders` is a VIEW on `order_tracker_xls`).
- **Product Insights:** HB1 and HB401 show higher conversion rates than average (confirmed via PIE findings).
- **Cem's Corrections:**
    - Veeqo connects to channels (it is not an API endpoint that n8n pushes to).
    - Drew's departure was due to resistance to AI and maintaining a bloated team.
- **Technical Reference:** `psql` path: `/opt/homebrew/Cellar/libpq/18.3/bin/psql`.

**Carry-forwards**
- **Veeqo Operations Spec:** Develop full spec covering Shopify international routing, CSV $\rightarrow$ PO pipeline, and migration mapping (70 manual processes $\rightarrow$ automated).
- **EOD Parser Phase 2:** Fix multi-person message splitting and SKU count accuracy.
- **Research:** Conduct deep research via Perplexity on specific Veeqo capabilities.
- **Data:** Execute currency normalization SQL in Supabase.