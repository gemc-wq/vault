# Daily Log — 2026-03-13 (Friday)

## Decisions
- **Repository Access:** Cem to provide Chad access to the new private repo `github.com/gemc-wq/zero-php` for PHP extraction.
- **EAN/UPC Strategy:** Due to the lack of EAN custom fields in BigCommerce, EANs will be sourced from Amazon Active Listings or via GS1 generation.
- **Bug Fix:** Cem to refresh the "US All Listings" file to resolve the PULSE Region Gap Finder bug (caused by stale data).

## Deliverables
- **RAG Pattern Cards:** Batch 11-30 completed and uploaded to `Brain/Projects/goheadcase/rag/patterns/`.
- **Currency Normalization:** Completed Supabase `orders` table backfill; `net_sale_usd` is now 100% complete across 319K+ rows.
    - **Migration Script:** `projects/data-migrations/001_currency_normalization.sql`
- **SKU Staging Pipeline:** Launched with 200 HTPCR champions identified via PULSE velocity data.
    - **Data Output:** `projects/sku-staging/htpcr_champions.json`
    - **Documentation:** `projects/sku-staging/SKU_STAGING_PLAN.md`
- **BigCommerce API:** Connection established for store `otle45p56l` (1.87M SKUs). Credentials stored in `TOOLS.md`.

## Blockers
- **Walmart Expansion:** Massive UPC/EAN gap identified; only 13.5% of identified HTPCR champions currently have UPC/EAN data.
- **Harry (Agent):** 4 tasks overdue (COGS, Chad file, BQ audit, Shopify connector); escalation required Monday.
- **Print Automation:** Awaiting EPS format documentation from the creative team.
- **Order Fulfillment:** Items `HLBWH-IPDA1324` and `HHYBK-IPH16` require "discon" tagging across all listing sites.

## Knowledge
- **BigCommerce API Schema:** Custom fields available for mapping: `BrandCode`, `DeviceBrand`, `DeviceModel`, `ProdCat`, `Product Type`, `TeamCat`, `DesignName`, `ParentSKU`, `GenericKeywords`, `EbayTitle`, `AmazonTitle`, `Product Device`, and `Price_US`.
- **Supabase Data Integrity:** The "160K null" PIE backfill issue is resolved via the sync script and currency backfill.
- **Process Insight:** Evita is autonomously developing AI-assisted tooling for PHP image validation and batch listing uploads.

## Carry-forwards
- **UPC Strategy:** Develop an acquisition or generation plan to bridge the UPC gap for Walmart.
- **PIE Phase 2:** Awaiting Cem's decision on the optimization tier for Walmart title optimization.
- **Task Escalation:** Address Harry's zero-deliverable streak on Monday.