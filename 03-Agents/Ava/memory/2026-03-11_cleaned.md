# 2026-03-11 — Daily Log

**Decisions**
- **Architecture Shift (Cem):** Move PULSE production to the Google stack (Cloud Run + BigQuery) to eliminate Supabase dependency and ensure sub-100ms latency.
- **Listings Strategy (Cem):** Prioritize individual child designs (e.g., `NARUICO-AKA`) rather than just top-performing lineups, as high-performing child designs drive the most value.
- **Optimization Logic (Cem):** Use the steepest portion of the revenue curve (before it flattens) as the indicator for the highest yield to SKU count ratio.

**Deliverables**
- **PRUNE App Scaffold:** Completed build in `projects/prune-app/` (Next.js 16, BQ integration). Live at: [https://prune-app-xi.vercel.app](https://prune-app-xi.vercel.app).
- **PULSE v2 Dashboard:** Deployed with new tabbed layout (Devices | Design Groups | Champions) and device-first hierarchy. Live at: [https://pulse-dashboard-v2.vercel.app](https://pulse-dashboard-v2.vercel.app).
- **Mission Statement:** Deployed to both [ecellglobal.com](https://ecellglobal.com) and [sales-dashboard-v2](https://sales-dashboard-v2.vercel.app).
- **Data Engineering:** 
    - Exported 457K rows of Amazon conversion data to BQ.
    - Created `v_pulse_design_performance` view (joins velocity, conversion, and coverage).
    - Downloaded 9.42 GB UK Active Listings to `data/amazon-uk/`.

**Blockers**
- **Miku Renewal:** Contract remains unsigned by Cem (9 days pending); Max Arguile is following up.
- **Harry COGS:** Delivery is overdue; blocking PULSE Phase 3 (profit scoring).
- **Amazon UK1:** Disbursement hold in effect; account verification/deactivation risk being handled by staff.

**Knowledge**
- **Technical Fixes (PULSE v2):** 
    - Resolved RLS timeouts by switching to server-side API routes using `SUPABASE_SERVICE_ROLE_KEY`.
    - Fixed schema mismatch in `lineups` table (`name` vs `label` and numeric FKs).
    - Resolved URL header overflow (22KB) by reducing batch size from 1000 to 100.
- **Algorithm:** Implemented Kneedle-inspired elbow detection in `src/lib/elbow.ts` to auto-select optimal device/design counts.
- **Security:** 9 vulnerabilities identified in Supabase project `auzjmawughepxbtpwuhe`.
- **Credentials:** Vercel token located in `gdrive:Clawdbot Shared Folder/Brain/Credentials/API Keys for Ava (1).txt`.
- **Revenue Insight:** `LFCLVBRD` design identified as a high-tier performer with 6.46% conversion ($54K revenue).

**Carry-forwards**
- **Data Integration:** Integrate UK/DE data into PRUNE (pending files from Cem).
- **Feature Development:** 
    - Implement Export button (generate Shopify CSV from Champions list).
    - Build Sales Velocity page (scatter plot + quadrant view).
    - Develop Pipeline tracker (Export $\rightarrow$ Shopify $\rightarrow$ Marketplace status).
- **Infrastructure:** Execute BQ migration for PULSE production.