# April 17, 2026

**Decisions**
- Reverted `/api/leaderboard` to use Supabase RPC instead of BigQuery to maintain architectural consistency with `/api/designs` and `/api/champions`.

**Deliverables**
- Resolved PULSE leaderboard outage by removing broken BigQuery client code and pushing fix commit `dbeef4f` to `gemc-wq/pulse-dashboard-v2`.

**Blockers**
- Vercel deployment protection is currently enabled, which may prevent public verification of the PULSE dashboard until an auth token is added or protection is disabled.

**Knowledge**
- **System Architecture:** PULSE dashboard views (leaderboard, designs, champions) should utilize Supabase RPCs for consistency.
- **Reporting Process:** For weekly Amazon analysis via Ava cron, download the following reports to `~/Downloads/` on Mac Studio every Saturday morning:
    - US Child ASIN Report (14-day)
    - US Child ASIN Report (30-day)
    - US Active Listings Report (if updated)

**Carry-forwards**
- Verify PULSE is publicly reachable once Vercel protection is addressed.
- Execute weekly Amazon report analysis on Saturday morning.