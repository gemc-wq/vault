# Daily Log: [Date]

## Decisions
- **Sync Strategy:** Approved by Cem to fix the existing sync rather than recoding to a hybrid BQ+Supabase approach.
- **Google Workspace CLI (gws):** Use existing Airweave OAuth client for now; migrate to `opsecellglobal` client later.
- **Inventory Dashboard:** Harry will build the dashboard and procurement layer on top of the existing Supabase `blank_inventory` table (no rebuild of the data layer required).
- **Inventory Logic:** `avg_daily_sales` in BQ represents global demand, not per-site; PH site does not print desk mats.
- **Sven Architecture:** Sven will remain a sub-agent under Ava (due to 8GB RAM constraints on the iMac) rather than a separate OpenClaw instance.
- **Research Routing:** Simple research tasks $\rightarrow$ Gemini 3.1 Flash; Complex research tasks $\rightarrow$ Gemini 3.1 Pro.

## Deliverables
- **`scripts/sync_bq_orders.py`:** Python script (478 lines) for BQ $\rightarrow$ Supabase delta sync. Supports `--backfill`, `--write` (dry-run default), and handles batching, FX conversion, and `Custom_Label` parsing.
- **Inventory Tracker v1:** 
    - Supabase schema: `blank_inventory` table and `v_inventory_alerts_v2` view.
    - Scripts: `setup_inventory_schema.py`, `snapshot_inventory.py`, `deduct_inventory.py`.
    - `v_inventory_global` view: Aggregates stock across all sites.
- **Handoffs:** `handoffs/harry-inventory-procurement-brief.md` created for Harry.
- **Research:** `research/openclaw-army-video-notes.md` (transcript and key takeaways).
- **Sven Task:** Completed 10 pattern cards and mega menu template.

## Blockers
- **Resolved:** `gog` CLI 403 error; replaced with `gws` CLI.
- **Resolved:** Supabase orders sync gap (identified one-time bulk load issue; fixed via new sync script).

## Knowledge
### Technical & Infrastructure
- **Hardware/Network:** 
    - Harry runs on iMac (`cems-imac`), not a VPS. 
    - Tailscale IPs: `cems-imac` (100.91.149.92), `cems-mac-studio` (100.72.19.27), `hp-cem` (100.120.86.40).
- **Google Workspace CLI (`gws`):** 
    - Version: v0.11.1 (installed via npm).
    - OAuth Client: `175143437106-funrdi7s4kh1n1sgefiln1v58fq6mkkg`.
    - Scopes: `drive.readonly`, `gmail.readonly`, `calendar.readonly`.
    - Credentials: `/Users/openclaw/.config/gws/credentials.enc` (AES-256-GCM).
- **Database:** 
    - Supabase connection: `postgresql://postgres:PASSWORD@db.auzjmawughepxbtpwuhe.supabase.co:5432/postgres` (Note: Session pooler does not work).
    - Inventory Data: 5,193 SKUs loaded; 1,210 active, 3,980 dead stock (77%).
    - Skin blanks (H89/H90) at 9,999 are roll stock, not discrete inventory.
- **Agent Roster & Hierarchy:**
    - Chain of Command: Cem $\rightarrow$ Ava $\rightarrow$ Harry.
    - Harry (GPT-5.4, iMac): Builder + Finance.
    - Ava (Sonnet 4.6, Mac Studio): Planner + Strategy.
    - Sven (Gemini 3.1 Pro, sub-agent): Creative + Design.
    - Forge/Spark (Codex GPT-5.3): Coding sub-agents under Ava.
- **Dashboards:** 
    - Blueprint (Kanban): `app-zeta-sable.vercel.app`
    - Command Center (Project Tracker): `command-center-one-puce.vercel.app`

### Process & Workflow
- **HEARTBEAT.md:** Now checks GDrive handoffs folder every heartbeat to auto-start work.
- **AGENTS.md:** Step 5 on session start now checks for new handoff briefs.

## Carry-forwards
- **Harry (Phase 1):** Build Dashboard + reorder suggestions + PO workflow (4 new tables). Target: Mar 19.
- **Harry (Phase 2):** Invoice/payment matching (3 new tables).
- **Sven:** 20 more pattern cards + benchmark captures + seed datasets.
- **Cleanup:** Remove Harry's legacy EOD cron.
- **Infrastructure:** Migrate `gws` to `opsecellglobal` OAuth client.