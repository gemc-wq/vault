# Daily Memory — 2026-04-01 (Wednesday)

## 8:30 AM — Weekly Backup + HB401 Sprint Wrap + NFL Flag
- Workspace backup completed: 5 GiB synced to GDrive (131 files transferred, 2,689 checked)
- Sent Cem: HB401 sprint results (partial success, 2 P1 designs missed) + NFL 3-day warning
- Removed HB401 Sprint Check from HEARTBEAT.md (sprint complete)
- Slack EODs confirmed: Chadle, Patricia, Jeffrey all delivering HB401 work through end of sprint
- Codisto subscription noted — validates Shopify→Walmart connector strategy

## Afternoon — Zero PHP Codebase Deep Dive

### Downloads & Data Acquired
1. **Zero PHP Codebase** — Full codebase extracted from `zero2-code.tar.gz` to `projects/zero-codebase/`
   - 58,107 files, 8,008 PHP files
   - Key directories: ZERO_2.0_BUILD_PLAN.md, ZERO_SYSTEM_SUMMARY.md, amazon/, barcode/, design-catalogue/, EcellEmployeeManagementSystem/, IT/, POAmalgamation/, REGEN/, ShopifyTestAPI/
2. **Key PHP files found:**
   - `wallpapers/wallpaper.class.php` — uses `replication.unit_measurements` for device categories
   - `POAmalgamation/amalgamate_po_upload_file_RON20151109.php` — uses `elcell_co_uk_barcode` DB
   - Legacy `getinfo.php` — reveals `t_jig_measurement` schema columns: `f_PhoneWidth`, `f_PhoneHeight`, `f_PhoneMarginX`, `f_PhoneMarginY`, `f_PaddingX`, `f_PaddingY`, `f_ImageRotation`, `f_CornerRadius`

### BQ / Jig Investigation
- **replication dataset tables confirmed:** TEMP_TAOBAO, asset, batch_rep, design_asset, device, headcase_designs, languages, t_jig_devices, t_jig_measurement, t_jigs, unit_measurements
- **unit_measurements:** 3,318 rows — contains modern devices (but queried via `UnitCode`, not device code format like IPH16PRO)
- **t_jig_devices:** 4,236 rows — but only legacy device codes (iPhone 4/5/6 era). NO modern IPH16PRO etc entries confirmed
- **t_jigs:** Has JigID, ProductCode, GroupCode, JigIndex, Manufacturer, Width, Height, MarginX/Y, OffsetX/Y, CountX/Y, Rotation
- **Key finding:** The `unit_measurements` table (3,318 rows) is the modern device dimensions store, NOT `t_jig_devices`. The jig system was updated to use `unit_measurements` for device specs.
- **wallpaper.class.php** queries `replication.unit_measurements WHERE UnitCode = '{device_code}'` — confirms `unit_measurements.UnitCode` IS the device identifier used in production PHP code
- **Historical 2014 SQL dump found:** `/Users/openclaw/Downloads/zero/sherlock/uploads/2014-05-21_16_24_28Jig Measurement.sql` — 196 device rows confirming 7200x4800 canvas scale

### ListingForge Pipeline Status (from prior sessions)
- Lane 1 POC v3 scaffolded with dynamic rotation + corner radius (Pillow)
- IT request drafted at `projects/listing-forge/JIG_ROW_REQUEST.md` — needs Cem to forward to Dickel/PH IT
- Layout math verified: legacy SQL coordinates correctly map to production canvas
- `ROTATION_CORNER_PLAN.md` created defining implementation order

## Evening — FBA Management App (Cem working separately)
- Cem appears to be working on an FBA management app at `~/Library/Mobile Documents/com~apple~CloudDocs/Desktop/Repos/Claude Code FBA/fba-management-app/`
- Has a Python venv set up — likely using Claude Code directly on that project
- No Ava involvement requested yet

## Key Blockers Active (as of end of day)
1. **NFL license EXPIRED Mar 31** — No renewal confirmation from Cem despite 4+ flags. CRITICAL.
2. **Jig IT request** — Cem needs to forward `JIG_ROW_REQUEST.md` to Dickel/PH IT for modern device SQL dumps
3. **Bulk Shopify upload (1,803 EAN-ready HB401 gaps)** — Ready, needs Cem go-ahead
4. **Jay Mark BQ credentials** — Still needed to unblock Evri fulfillment portal
5. **CLC royalty reports overdue** — Breach risk

## Cron & Model Policy (confirmed active)
- All crons on free models (pixel=gemini-flash, bolt=gemini-pro)
- Ava (main) = Sonnet 4.6 (Anthropic API)
- Heartbeat already on gemini-3-flash-preview ✅

## EOD SUMMARY

### What we accomplished today
- Completed weekly workspace backup (5 GiB synced to GDrive).
- Wrapped up the HB401 sprint and confirmed team deliverables.
- Executed Zero PHP Codebase deep dive (analyzed 58k+ files).
- Discovered `unit_measurements` table is the modern device spec store (superseding `t_jig_devices`).
- Verified ListingForge layout math against legacy SQL dumps.

### Open items / carry-forwards
- **CRITICAL:** NFL license expired Mar 31. Needs immediate renewal.
- Forward `JIG_ROW_REQUEST.md` to Dickel/PH IT for modern device dimensions.
- Execute bulk Shopify upload for 1,803 EAN-ready HB401 gaps.
- Obtain Jay Mark's BQ credentials for Evri fulfillment portal.
- Complete overdue CLC royalty reports to avoid breach risk.

### Notable context
- Cem is developing an FBA management app independently via Claude Code.
- Codisto subscription is active, validating the Shopify→Walmart connector strategy.
