# 2026-04-03

- Cem corrected the memory architecture: the canonical model is now **4 layers**, not the prior simplified 3-layer framing.
- Canonical 4-layer model:
  1. Built-in memory = tiny auto-injected compact facts/pointers.
  2. Injected operating files = AGENTS.md + SOUL.md and other behavior/rules files.
  3. Obsidian Vault = durable continuity/reference workhorse, read on session start/after compaction/during work; written during checkpoints, corrections, task progress, and completion.
  4. Session search = automatic archival recall / safety net.
- Critical correction from Cem: `TOOLS.md` and `MEMORY.md` remain in each agent’s **primary workspace infrastructure** and are **not** part of the Obsidian vault. The vault is layer 3 only.
- Wiki remains the canonical business/source-of-truth layer; vault is continuity/reference, not a competing wiki.
- `VAULT_OPERATING_SYSTEM.md` was created to reflect the corrected architecture and explicitly state that the workspace remains the agent’s operational home.
- User intent: proceed to the next stage of vault implementation/documentation before full rollout.
