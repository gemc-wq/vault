# Daily Memory — 2026-04-02 (Thursday)

## Decisions
- **IREN/DRECO Modernization:** Reframed to focus on two lanes; Cem directed priority to **Lane 2 (Print Production)** first.
- **Orders Sync:** Harry's BQ→Supabase sync (using `on_conflict=sales_record_number` upsert) is now the sole source of truth; Ava/pixel duplicate cron removed.
- **Knowledge Management:** 
    - The Obsidian vault is the 3rd layer of memory (continuity/reference).
    - `wiki/` is the single source of truth for business knowledge and project documentation.
    - **4-Layer Memory System:** 1. Built-in memory, 2. Injected operating files (`AGENTS.md`, `SOUL.md`), 3. Obsidian vault, 4. Session-search archive.
    - Agent `TOOLS.md` and `MEMORY.md` must remain in the primary workspace, not the vault.
- **Vault Structure:** Implemented new directory scaffold: `vault/00-shared/`, `vault/01-ava/`, `vault/02-harry/`, and `vault/03-hermes/`.

## Deliverables
- **Technical Audits:** Completed readiness assessment for HWBT (Bottle) and HMWA (Wall Art) product lines.
- **Project Documentation:** Created Master Project Brief (`wiki/35-iren-dreco/MASTER_PROJECT_BRIEF.md`) and architecture docs (`VAULT_ARCHITECTURE.md`, `VAULT_OPERATING_SYSTEM.md`).
- **Data Analysis:** 
    - 60-day BigQuery sales pull completed; identified HLBWH ($1.07M) and H5TPCR ($1.03M) as primary revenue drivers (92% of top 3).
    - Deep dive into legacy royalty reporting logic completed, mapping fragmented layers and proposing a new schema.
- **Infrastructure:** Loaded Jig data into local MySQL (`t_jig_devices`, `t_jig_measurement`, `t_jigs`).

## Blockers
- **Resolved:** 
    - **NFL Renewal:** Not an immediate emergency; 90-day sell-off window is active and renewal is in progress.
    - **Jig IT Request:** Resolved via local MySQL load.
    - **Supabase Table Creation:** SQL executed via dashboard to address missing target tables.
- **Active:** 
    - **SKU Approval:** Awaiting Cem's final sign-off on codes (`HWBT24`, `HWBL32`, `HMWA34`) in `projects/new-products/SKU_STRUCTURE.md`.
    - **Supabase Infrastructure:** Harry to create missing tables: `cfx_products`, `cfx_devices`, `cfx_designs`, `cfx_ean_lookup`.

## Knowledge
- **NFL License Status:** Expired Mar 31. Annualized risk: $97K. Key contacts: Casey Collins (SVP, Consumer Products) and Renie Anderson (EVP & CRO). Japan (Rakuten) represents 69% of volume.
- **Product Launch Specs:** Wall Art is ready for ListingForge. Water Bottles require Pillow wrapper logic for taper/wrap adjustments.
- **LAZCUT:** EPS cutfile library is organized by location and keyed by `PRODUCT_TYPE-DEVICE.eps`.
- **Royalty Reporting Architecture:** Logic is fragmented across three layers:
    1. **Contract Master Data:** MySQL tables (`t_royalty_information`, `t_royalty_rates`).
    2. **Legacy Engine:** PHP (`projects/zero-codebase/barcode/get_1d_sales.phpDREW042418`) handles net sales/GBP conversion and SKU/Description rewrites.
    3. **JSON Configs:** Located in `/Users/openclaw/Downloads/zero/royalty-app/app/report_configs/`.
    - *Note:* Internal SKU parsing cannot be reused for royalty exports due to separate licensor-facing taxonomies and external master CSVs (e.g., LFC).
- **Proposed Royalty Schema:** `royalty_licenses`, `royalty_rates`, `royalty_territory_mappings`, `royalty_channel_mappings`, `royalty_sku_rules`, `royalty_description_rules`, `royalty_reference_skus`, `royalty_output_templates`.

## Carry-forwards
- **Ava:** Populate royalty rates and finalize COGS calculator.
- **Cem:** Send product files for storage in `projects/new-products/`.
- **Harry:** Execute Lane 2 modernization (Priority: HLBWH $\rightarrow$ HTPCR $\rightarrow$ HC) and draft canonical royalty schema/replication spec.