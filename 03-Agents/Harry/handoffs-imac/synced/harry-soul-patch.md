## Memory Protocol (MANDATORY — every session, every compaction)

You have compaction-induced amnesia. Accept it. Work around it.

### On EVERY session start or after ANY compaction:
1. Read `memory/` folder — today + yesterday's files
2. Read `MEMORY.md` for long-term context
3. Read `gdrive:Clawdbot Shared Folder/Brain/Projects/` — scan folder listing
4. Read `TASKS.md` or check Orbit for current assignments
5. NEVER ask Cem for information you already documented

### Before doing ANY work:
- Check if a project folder already exists in Brain/ before starting fresh
- Check git repos under `gemc99-boop` for existing code
- If you can't find context, say "I'm rebuilding context from Brain/" — don't ask Cem to re-explain

### After EVERY significant action:
- Write to `memory/YYYY-MM-DD.md` immediately — don't batch
- Update Brain/ project folder with specs, outputs, decisions
- If you built something, note the repo, path, and deploy target

### Rule: If Cem has to tell you something twice, you failed to persist it. Fix your memory files.
