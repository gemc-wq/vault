# Memory Log - 2026-04-13

## Procurement System - Supabase Migration

**Status:** Code migration complete, Vercel deploy blocked

### What Was Done
- Converted all API routes from `pg` (PostgreSQL) to Supabase JS client:
  - `app/api/po/route.ts`
  - `app/api/po/create-batch/route.ts`
  - `app/api/reorder/route.ts`
  - `app/api/reorder/[id]/route.ts`
  - `app/api/reorder/generate/route.ts`
- Updated `lib/db.ts` to export Supabase client
- Simplified `lib/migrate.ts` (migrations already applied)
- Updated `.env.local` and `.env.production` with correct Supabase credentials

### Local Build
✅ Passes TypeScript compilation and Next.js build

### Vercel Deploy
❌ Failing with "Unexpected error" - appears to be a platform/infrastructure issue, not code-related. Multiple retry attempts failed.

### Next Steps (Pending)
1. Set environment variables in Vercel dashboard manually, OR
2. Deploy to Cloud Run instead (more reliable for this app), OR
3. Wait and retry Vercel deploy later

---

## Inventory Sync Script - Fixed

**Updated:** 2026-04-13 07:30 EDT

### Problem
Cron job `BQ→Supabase inventory sync (UK+FL)` was using stale tables:
- ❌ `production_tracker.t_uk_stocks`
- ❌ `production_tracker.t_fl_stocks`

### Solution
Updated `/Users/clawdbot/.openclaw/workspace/scripts/bq_to_supabase_inventory_sync.mjs` to use canonical source:
- ✅ `zero_dataset.inventory` (BigQuery VIEW)

### Changes Made
- Rewrote queries to use `zero_dataset.inventory` with correct column names
- Added support for all 3 warehouses: UK, Florida, PH
- Added `--max_rows=100000` flag to retrieve all items (not just 100)
- Excludes items with `z-` or `Z-` prefix per documented rule
- Maps BigQuery columns to Supabase schema correctly

### Verification
Test run completed successfully:
- UK: 100 items synced
- Florida: 100 items synced  
- PH: 100 items synced

### Reference
See wiki: `/Users/clawdbot/.openclaw/workspace/wiki/inventory-ordering-app/PROJECT.md` Section 10 for canonical data source documentation.

---
*Logged during pre-compaction flush*
