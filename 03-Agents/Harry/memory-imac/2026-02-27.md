# 2026-02-27

## Durable notes (compaction-proof)

### Shared Brain / autonomy plan (Cem direction)
- Cem reiterated the intended operating model: a **shared “Brain” on Google Shared Drive** (via rclone) used by both Harry and Ava, so project state survives compaction/restarts.
- Need a central minimal “root” doc listing: rclone remote/roots, active projects, how to sync, key constraints—so agents always re-orient after restart.
- Cem created a **Telegram group** to enable Harry + Ava to coordinate without Cem being the message router; need to ensure both bots can read/respond in group.

### rclone / Shared Drive canonical paths
- rclone remote: `gdrive:`
- canonical root: `gdrive:Clawdbot Shared Folder/`
- canonical projects root: `gdrive:Clawdbot Shared Folder/Brain/Projects/`
- local mirror (Windows): `C:\Users\gemc\clawd\gdrive_shared\`

### Sales Dashboard link issue
- Sales Dashboard tile in `business-dashboard` was still pointing to old GitHub Pages URL `https://gemc-wq.github.io/sales-dashboard/`.
- Fix is to point tile to internal route `/apps/sales-dashboard` and redeploy Cloud Run (note: user later reported it still not updating—likely cache or wrong host).

### NBCU PO → FedEx CSV
- Generated a minimal CSV with only 3 POs (from PDFs 263/264/265): `nbcu_fedex_only_3pos_0p5lb_11x7x1.csv`.
- Uploaded to Drive: `gdrive:Clawdbot Shared Folder/Brain/Projects/nbcu-po-automation/nbcu_fedex_only_3pos_0p5lb_11x7x1.csv`.

### Ava iMac outage + recovery
- Ava gateway went down due to invalid config option: `channels.telegram.groupPolicy` expects one of `open|disabled|allowlist`.
- Folder `.openclaw` is hidden on macOS; use Finder “Go to Folder” (`Cmd+Shift+G`) to open `/Users/clawdbot/.openclaw`.
- After editing config and restarting, `openclaw status` shows gateway running; Telegram/Discord channels OK; memory vector ready.
- Cem shared a screenshot that exposed Ava’s Telegram bot token—recommend rotating later.
