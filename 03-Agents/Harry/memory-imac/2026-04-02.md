# 2026-04-02

## Ava Mac Studio Tailscale details saved
- Cem shared screenshot with Ava Mac Studio Tailscale target details
- MagicDNS: `cems-mac-studio.tail461b28.ts.net`
- IPv4: `100.72.19.27`
- Screenshot status showed **Not Connected** at the time
- Updated `MEMORY.md` with current Ava host / SSH targets

## Xero app inspection context
- Cem asked Harry to verify April 2026 Xero granular scope changes against the local `Finance/xero-query-app`.
- Local app already supports UK + US multi-org env structure, token refresh, organisation check, tax rates, invoice creation, AP bill creation via Xero Invoices API, and invoice email send.
- Current app files do **not** yet include an explicit Xero auth URL/scopes builder or granular-scope-aware reauth flow.
- Working conclusion: app is not blocked, but should be updated to explicit granular scopes + reauthorization flow.

## Xero app screenshots from Cem
- Cem shared Xero developer dashboard screenshots showing apps: `Ecell UK`, `Ecell US`, `Ecell US Write`, plus an `AI Agent` custom connection.
- Detailed screenshots received for **Ecell US** confirmed:
  - App type: `Web app`
  - Redirect URI: `http://localhost:3000/callback`
  - Client secret created: `2026-03-11`
  - Connected tenant: `Ecell Global Inc.`
  - Xero UI displayed the granular-scope transition notice.
- Because the visible `Ecell US` secret/app is post-2026-03-02, Harry concluded it should be treated as operating in the new granular-scope era rather than relying on legacy broad-scope assumptions.
- Next requested action from Cem: open and inspect **`Ecell US Write`** with the same internal screens to determine whether that is the correct finance posting app for bill/journal write flows.

## Xero US Write app chosen for new write flow
- Cem opened **Ecell US Write** and shared the internal screens.
- Confirmed from screenshots:
  - App type: `Web app`
  - Redirect URI: `http://localhost:3000/callback`
  - Client ID: `A09BF4DD7CB140398D41F9D8FC29EEAF`
  - Connections: `0 of 5`
- Conclusion: `Ecell US Write` is the clean app to use for new finance write access because it has no existing tenant connections yet.

## Xero US Write secret + auth callback captured
- Cem generated and shared the `Ecell US Write` client secret for setup.
- Cem completed the Xero authorization in browser and shared callback URL containing a short-lived auth code for `state=ecell-us-write`.
- Harry confirmed the app can run now with the existing backend scaffold, even though a polished in-app OAuth UX is still missing.

## Finance app run/test status on 2026-04-02
- Checked local `Finance/xero-query-app` and confirmed it is a real working Express app, not just a stub.
- Installed missing npm dependencies via `npm install` because the code-exchange script initially failed on missing `dotenv`.
- Tried exchanging the captured `Ecell US Write` auth code later, but Xero returned `invalid_grant / Authorization code not found` because the code had expired.
- Started the app locally and verified these routes work against the currently configured US org:
  - `/config-check`
  - `/xero/us/organisation`
  - `/xero/us/tax-rates`
- These checks confirmed the existing configured tenant is **Ecell Global Inc.** and current tokens allow read/settings access.

## Invoice import test result using Google Cloud invoice
- Cem sent a real Google Cloud invoice PDF for testing:
  - Invoice number: `5542517036`
  - Invoice date: `2026-03-31`
  - Supplier: `Google LLC`
  - Total: `USD 76.25`
  - Description: `Google Cloud - Fee for March 2026`
- Harry tested posting it into Xero through the app’s `/api/intake/xero/bill` route as a draft bill.
- Result: Xero returned `AuthorizationUnsuccessful`.
- Key conclusion: parsing is not the blocker; current app credentials/token set appear to support read routes only, while write/bill creation is failing.
- Likely root cause: app `.env` is still using the older `XERO_US_*` app credentials/tokens instead of the new `Ecell US Write` credentials and a fresh token set.

## Next-session starting point
- Immediate next step is to switch `Finance/xero-query-app/.env` from the legacy US app credentials to the `Ecell US Write` client ID/secret, re-authorize, capture a fresh auth code, exchange it immediately, then rerun the same Google invoice draft-bill import test.
- Cem explicitly asked Harry to save work/memories so a new session can continue from this point.
